[phases.setup]
nixPkgs = ["python312", "python312Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "celery -A services.celery_app.celery_app worker --loglevel=info --queues=analysis,watchdog --concurrency=2 --max-tasks-per-child=50"
